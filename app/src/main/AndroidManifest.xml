<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SharedBalance"
        tools:targetApi="31">
        <activity android:name=".screens.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".screens.home.HomeActivity" />
        <activity android:name=".screens.register.RegisterActivity" />
        <activity android:name=".screens.profile.ProfileActivity" />
        <activity android:name=".screens.expenses.ExpensesActivity" />
        <activity android:name=".screens.history.HistoryActivity" />
        <activity android:name=".screens.creategroup.CreateGroupActivity" />
        <activity android:name=".screens.account.AccountActivity" />
        <activity android:name=".screens.settings.SettingsActivity" />
        <activity android:name=".screens.landing.LandingActivity" />
        <activity android:name=".screens.expenses.details.GroupDetailsActivity" />
        <activity android:name=".screens.expenses.calculate.CalculateExpenseActivity" />
        <activity android:name=".screens.expenses.breakdown.ExpenseBreakdownActivity" />
        <activity android:name=".screens.profile.editprofile.EditProfileActivity" />
    </application>

</manifest>